package com.lambdaschool.webemployees;

public interface CheckEmployee
{
    boolean test(Employee e); // the method inside of the for loop
}

// We're going to use this interface for the lambda expression to filter the Employee arrayList
